function maximo(n1,n2){
    return n1>n2 ? n1 : n2
    /*
    Aca usamos un operador ternario. Funcionan de la siguiente manera:
    condicion ? valorTrue : valorFalse
    
    Ejemplo:
    esAdmin ? "Acceso concedido" : "Acceso denegado"
    
    Se puede leer también así:
    if(esAdmin){
        return "Acceso concedido"
    }
    else{
        return "Acceso denegado"
    }
    */
}